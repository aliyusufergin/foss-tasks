{
  "//": "Node-safe seams (data-layer logic + auth client) and their tests. Runs in CI and locally without the React Native/Expo toolchain installed. Pure domain logic now lives in packages/domain and typechecks there; this project resolves it through its built dist/, so run the domain build first. The full app typechecks via tsconfig.json (npm run typecheck:app) on a dev machine.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022"],
    "types": ["node"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "verbatimModuleSyntax": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": [
    "src/data/model.ts",
    "src/data/migrations/**/*.ts",
    "src/data/queries.ts",
    "src/auth/client.ts",
    "src/auth/token-store.ts",
    "src/theme/color.ts",
    "src/theme/compile.ts",
    "src/theme/tokens.generated.ts",
    "src/prefs/store.ts",
    "src/prefs/theme-preference.ts",
    "src/prefs/language-preference.ts",
    "src/i18n/en.ts",
    "src/i18n/tr.ts",
    "test/**/*.ts"
  ]
}
