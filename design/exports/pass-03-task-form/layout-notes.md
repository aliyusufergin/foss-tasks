# Task detail / create form — layout notes
Full screen on bg.base. Header: 40 back chevron + type.title centered + Save accent text-button.
Column gap space.md, paddingH space.lg.
Title: 20/600 borderless input over bottom hairline.
Quick chip row: gap space.sm — set values = accent.surface pills with ×; unset = bg.subtle pill + chevron (date, priority).
Description: bg.subtle field, type.body text.secondary.
Sub-tasks: caption header n/total + checklist card (pass-01 component; nest = paddingLeft 26/level) + accent "Add item" row.
Optional fields: "+ Add …" rows, 44 high, hairline separated (recurrence, reminder, defer).
Same form = create (empty) / edit / AI preview (pre-filled). Feature-toggled-off fields are not rendered at all.