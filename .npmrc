# The Expo/React Native dependency graph has peer-dep conflicts that npm refuses
# by default (the app previously installed with an explicit --legacy-peer-deps).
# Setting it here keeps a bare `npm install` / `npm ci` working at the workspace
# root, including in CI, rather than relying on every caller remembering a flag.
legacy-peer-deps=true
