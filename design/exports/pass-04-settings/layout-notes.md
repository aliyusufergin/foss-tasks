# Settings — layout notes
Pushed screens: header = 40 back chevron + type.title centered + 40 spacer; no tab bar.
Grouped cards: bg.surface, radius.lg, hairline border; rows padding 13/14, hairline between — label 15/500 flex-1 + value 13 text.muted + 14 chevron-right. Group captions 11/700 letterSpacing 0.08em text.muted.
Toggle: 44×26 pill — on: accent track, knob accent.on right; off: faint track, knob left.
Radios as in pickers (selected = 6-wide accent ring).
Theme rows: 28 radius.md preview swatch; custom theme = dashed import row (DTCG JSON, themeVersion-validated; unknown tokens ignored).
AI config: Cloud / Self-hosted radios; self-hosted reveals mono bg.subtle URL field indented 32.
Feature toggles: AI capture, Descriptions, Reminders, Recurrence, Defer — off = field disappears everywhere; core (title, schedule, done, sub-tasks) always on.
Account: info card + full-width bordered Sign out (label status.overdue). Footer caption: version + last sync.